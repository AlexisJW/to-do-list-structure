import Todo from '../modules/todo.js';
import UI from '../modules/ui.js';
import './style.css';

document.addEventListener('DOMContentLoaded', UI.displayTodos);

// const todo = new Todo(title, author, id);

// UI.addBookList(book);

// sstore.addBook(book);